import log

# Set one or both args to True to initialise
# These functions initialise the log tables, insert and
# print a test row, then delete it leaving the table empty
# WARNING: deletes all data

log.init(False)
log.init_byhour(False)
